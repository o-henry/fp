describe("TEST CHAPTER 04", () => {
    function score(word: string): number {
        return word.replace("a", "").length;
    }

    it("should_score_each_word_in_the_list_based_on_an_function_score", () => {
        const words = ["ada", "haskell", "scala", "java", "rust"];
    });
});
export {};
